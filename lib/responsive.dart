class res {
  static dynamic responsiver(input, target, percentage) {
    if (input == "h") {
      return target * (percentage / 100);
    } else {
      return target * (percentage / 100);
    }
  }

  static dynamic autoFont(h, percentage) => h * (percentage / 100);
}
